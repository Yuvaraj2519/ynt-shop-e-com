spring:
  application:
    name: app
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: q1w2e3r4
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 9090

